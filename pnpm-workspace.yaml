# The CLI (root) and the browser app (app/) share one install so the app reuses
# the repo's src/car-url.ts with the exact same multiformats / data-segment
# versions the CLI commits over — no version drift between them.
packages:
  - app
