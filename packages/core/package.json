{
  "name": "ipfs2foc-core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared canonical CAR helpers for ipfs2foc — the single source of truth for the gateway CAR URL the CLI commits over and the relay/app redirect to, and for the canonical CAR export those commitments are computed from. Consumed (bundled) by the CLI, relay, and browser app.",
  "exports": {
    ".": "./src/car-url.ts",
    "./car-export": "./src/car-export.ts"
  },
  "dependencies": {
    "@ipld/car": "^5.4.0",
    "multiformats": "^13.4.1"
  }
}
